export { ensureBoringCache, execBoringCache } from '@boringcache/action-core';
export {
  validateInputs,
  parseEntries,
  resolvePath,
  resolvePaths,
  getCacheConfig,
  getPlatformSuffix,
  getInputsWorkspace as getWorkspace,
  convertCacheFormatToEntries,
  CacheEntry,
  CacheConfig,
} from '@boringcache/action-core';
