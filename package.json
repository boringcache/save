{
  "name": "boringcache-save-action",
  "version": "1.0.0",
  "description": "Save portable cache artifacts for GitHub Actions. Reuse anywhere. 3x faster than actions/cache/save.",
  "main": "dist/save-only/index.js",
  "scripts": {
    "build": "tsc && ncc build lib/save-only.ts -o dist/save-only",
    "package": "ncc build lib/save-only.ts -o dist/save-only",
    "test": "jest --passWithNoTests"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/boringcache/boringcache.git"
  },
  "keywords": [
    "actions",
    "cache",
    "boringcache"
  ],
  "author": "BoringCache",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "@types/node": "^20.19.10",
    "@vercel/ncc": "^0.38.1",
    "jest": "^29.0.0",
    "ts-jest": "^29.4.1",
    "typescript": "^5.0.0"
  }
}
